{
  "name": "TeacherDoc",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/TeacherDoc",
  "projectType": "application",
  "tags": [],
  "// targets": "to see all targets run: nx show project TeacherDoc --web",
  "targets": {
    "prisma:generate": {
      "executor": "nx:run-commands",
      "options": {
        "command": "prisma generate",
        "cwd": "apps/TeacherDoc"
      }
    },
    "prisma:migrate": {
      "executor": "nx:run-commands",
      "options": {
        "command": "prisma migrate dev",
        "cwd": "apps/TeacherDoc"
      }
    },
    "prisma:studio": {
      "executor": "nx:run-commands",
      "options": {
        "command": "prisma studio",
        "cwd": "apps/TeacherDoc"
      }
    },
    "prisma:push": {
      "executor": "nx:run-commands",
      "options": {
        "command": "prisma db push",
        "cwd": "apps/TeacherDoc"
      }
    },
    "build": {
      "executor": "@nx/next:build",
      "outputs": ["{options.outputPath}"],
      "dependsOn": ["prisma:generate"],
      "options": {
        "root": "apps/TeacherDoc",
        "outputPath": "dist/apps/TeacherDoc"
      }
    },
    "dev": {
      "executor": "@nx/next:server",
      "dependsOn": ["prisma:generate"],
      "options": {
        "buildTarget": "TeacherDoc:build",
        "dev": true
      }
    },
    "test": {
      "dependsOn": ["prisma:generate"]
    },
    "lint": {
      "dependsOn": ["prisma:generate"]
    }
  }
}
